node_modules
dist
.output
.vinxi
pnpm-lock.yaml
package-lock.json
bun.lock
routeTree.gen.ts

# Tool-managed and generated — reformatting these creates churn and can
# confuse the tools that own them.
.lovable/
src/integrations/supabase/types.ts
AGENTS.md

# SQL harness is formatted for readability by hand
neuvto-harness/

# Regenerated by the TanStack/Lovable vite plugin whenever the config loads
# (running vitest is enough). Formatting them is futile — the plugin rewrites
# them unformatted on the next run, and CI only passed before because `lint`
# happened to run before `test` in the same job.
# Matched by filename because [.mcp] is a character class in ignore-file syntax.
**/list-tools.ts
**/invoke-tool/**
**/oauth-protected-resource.ts
src/routes/mcp.ts
